package plugin

import (
	// "fmt"
	x "gobot/utils/message"
	y "gobot/utils/simple"
	// pm2 "github.com/unitech/pm2-go-api"
	)
	
func Dashboard(ball *y.S, m *x.Parse)  {
	// client, err := pm2.NewClient()
	// if err != nil { ball.Reply(fmt.Sprintf("Client error at: %s\n", err)) }
	// procs, err := client.List()
	// if err != nil { ball.Reply(fmt.Sprintf("Get list error at: %s\n", err)) }
	// for _, proc := range procs {
	// 	ball.Reply(fmt.Sprintf("Nama proses: %s, Penggunaan memori: %d MB\n-----------------\n", proc.Name, proc.Monit.Memory/1024/1024), true)
	// }
}